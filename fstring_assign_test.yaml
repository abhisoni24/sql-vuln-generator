rules:
  - id: fstring-assign
    patterns:
      - pattern: |
          $VAR = request.args.get(...)
          ...
          $QUERY = f"...{$VAR}..."
          cursor.execute($QUERY, ...)
    message: "SQL injection: f-string assigned to variable then executed"
    severity: ERROR
    languages: [python]
