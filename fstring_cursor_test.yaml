rules:
  - id: fstring-cursor
    patterns:
      - pattern: |
          $VAR = request.args.get(...)
          ...
          cursor.execute(f"...{$VAR}...", ...)
    message: "cursor.execute with f-string containing request variable"
    severity: ERROR
    languages: [python]
